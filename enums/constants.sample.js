const PUBLIC_MEMBER_DATA_CSV_LINK = "";

const EMAIL_STATUS = {
    PENDING: "pending",
    SUCCESS: "success",
}

const GMAIL_CONFIG = {
    email_address: "",
    password: "",
    username: ""
}

const PREVIEW_MODE = true

module.exports = {
    PUBLIC_MEMBER_DATA_CSV_LINK,
    EMAIL_STATUS,
    GMAIL_CONFIG,
    PREVIEW_MODE
}